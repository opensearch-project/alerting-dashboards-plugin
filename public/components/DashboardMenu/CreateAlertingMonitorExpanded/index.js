import React from 'react';
import {
  EuiContextMenu,
  EuiLink,
  EuiText,
  EuiHorizontalRule,
  EuiCallOut,
  EuiSpacer,
  EuiPanel,
} from '@elastic/eui';

const CreateAlertingMonitorExpanded = ({ setMonitors, setAlerts }) => (
  <div>SCREEN 6 & 7. CreateAlertingMonitorExpanded goes here</div>
);

export default CreateAlertingMonitorExpanded;
